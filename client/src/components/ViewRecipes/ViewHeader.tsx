import React from 'react'
import {IRecipe} from '../../types/RecipeType'

const ViewHeader: React.FC<IRecipe> = ({title, description, images, servings, prepTime, cookTime, postDate, editDate}) => {
  return (
    <div>
      
    </div>
  )
}

export default ViewHeader
